{
  "name": "jsx-rendering-engine",
  "version": "0.0.1",
  "description": "wherein I attempt to make a lightweight server-side rendering enging for TypeScript",
  "main": "index.ts",
  "author": "Chris Ronning <christopher.a.ronning@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.6.2"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "shelljs": "^0.8.3",
    "ts-jest": "^24.0.2"
  },
  "scripts": {
    "build": "yarn clean && npx tsc",
    "clean": "npx shx rm -rf ./out/** && mkdir out && npx shx touch ./out/.gitkeep",
    "build:test": "yarn build && cd test && yarn build"
  }
}
